#!name=香蕉VPN
#!desc=提取节点

[MITM]
hostname = %APPEND% api.banala.top, api.banala.icu, api.banala.shop

[Script]
# 拦截返回内容并执行处理脚本
香蕉VPN节点提取 = type=http-response,pattern = ^http://api\.banala\.(top|shop|icu)/api/frostkiss